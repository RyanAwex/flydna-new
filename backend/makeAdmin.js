const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect('mongodb://localhost:27017/flydna').then(async () => { let u = await User.findOne({ email: 'admin@flydna.io' }); if(!u) { u = new User({ _id: 'admin_123', email: 'admin@flydna.io', password: 'Password123!', name: 'Admin', isAdmin: true }); } else { u.password = 'Password123!'; u.isAdmin = true; } await u.save(); console.log('Admin ready'); process.exit(0); });
